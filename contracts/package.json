{
  "name": "saypay-contracts",
  "private": true,
  "description": "SayPayVault: seed-phrase-free wallet with guardian recovery and an inheritance switch",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "abi": "node scripts/export-abi.js",
    "tester": "node scripts/serve-tester.js",
    "wallets": "node scripts/make-demo-wallets.js",
    "verify:sepolia": "hardhat run scripts/verify.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "dotenv": "^18.0.4",
    "hardhat": "^2.29.1",
    "solc": "^0.8.28"
  }
}
